{
  "name": "cdrl-m01-data-contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "M01 data contract and reproducible PostgreSQL environment for CDRL.",
  "scripts": {
    "setup:db": "docker compose up -d postgres && node scripts/wait-for-postgres.mjs && node scripts/migrate.mjs && node scripts/seed.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && node scripts/verify.mjs",
    "run": "node scripts/run.mjs"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
